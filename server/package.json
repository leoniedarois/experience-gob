{
  "name": "server",
  "version": "1.0.0",
  "description": "to store data from canvas",
  "main": "server.js",
  "scripts": {
    "start": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "leoniedarois",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
